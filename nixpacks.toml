[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm ci --only=production']

[phases.build]
cmds = ['echo "Build phase completed"']

[start]
cmd = 'npm start'

[variables]
PORT = "3000"